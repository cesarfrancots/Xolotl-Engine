Original prompt: the goal will be to implement your idea thats achievable and then grow it slowly, how does that sound?

Progress:
- Built the first text-first engine slice: JSON scene, deterministic core, CLI checks/tests, and browser canvas runner.
- Next useful step: expose browser hooks so agents can inspect and advance the game deterministically.
- Added browser hooks: `window.render_game_to_text()` and `window.advanceTime(ms)`.
- Browser smoke found only an implicit favicon 404; fixed with a data favicon.
- Verified browser hook smoke in installed Chrome: `advanceTime(1000)` moved player x from 1 to 5 with no console errors.
- Added stricter test expectations for final entity fields and messages.
- Added a collision regression fixture and let the CLI run multiple test specs in one command.
- Tightened `reach_exit` around actual overlap behavior: win triggers when the player first overlaps the goal.
- Updated `advanceTime(ms, actions)` so agents can pass explicit input instead of relying on live keyboard state.
- Verified explicit browser stepping: `advanceTime(1000, { right: true })` moves player from x=1 to x=5 with no console errors.
- Added `npm run describe` so agents can inspect supported actions, entity kinds, scene fields, and test fields.
- Made `check` and `test` discover all JSON files by default, so new scenes/fixtures are automatically covered.
- Added `npm run trace` for per-step fixture state, and fixed `run` after CLI arg discovery changed.
- Fixed `dumpState` snapshots so trace steps do not share the live messages array.
- Made `trace` emit one JSON array so agents can parse multi-fixture output directly.
- Added exact `frames` test steps and `expect.tick`, then moved current fixtures off decimal seconds.
- Verified browser smoke after frame/tick changes. The skill Playwright script still cannot run from project Node because local `playwright` is not installed; Node REPL Playwright + installed Chrome works.
- Added scene bounds validation and a validation-error fixture so AI-authored scenes cannot place entities off-world unnoticed.
- Added `npm run verify`, a single JSON gate covering all scene validation and all test fixtures.
- Added `npm run solve`, a small grid solver that verifies generated win paths through the real simulation.
- Verified solver output: it finds the two-step `right`/`down` route for `level_01` and `verify` now includes that playable solution.
- Added validation for player/goal overlap with solids and a fixture proving the error shape.
- Verified overlap validation through `verify`, `test`, `solve`, `trace`, and browser smoke.
- Added validation for invalid `speed` values and a fixture proving the error shape.
- Verified speed validation through `verify`, `check`, `test`, `describe`, `solve`, `trace`, and browser smoke. The skill Playwright client still lacks local `playwright`; Node REPL Playwright + installed Chrome works.
- Added a `hazard` primitive, a losing fixture, and a hazard scene that proves the solver routes around danger.
- Added a `collectible` primitive, score expectations, a coin room scene, and validation for invalid collectible values.
- Verified collectibles through `verify`, `check`, `test`, `describe`, `solve`, `trace`, and browser smoke. The skill Playwright client still lacks local `playwright`; Node REPL Playwright + installed Chrome works.
- Added score to the browser HUD so the visible game state matches `render_game_to_text`.
- Verified HUD score with `verify`, `test`, and browser smoke: coin pickup changes both HUD and text state from score 0 to 5.
- Added score-gated goals with `requiresScore`, including a locked-exit scene, blocked/win fixtures, validation for invalid required scores, and solver routing through enough collectible value before ending.
- Verified locked exits through `verify`, `check`, `test`, `describe`, `solve`, `trace`, and browser smoke. The skill Playwright client still lacks local `playwright`; Node REPL Playwright + installed Chrome works.
- Added `npm run schema`, which prints JSON Schema for scene and test authoring so agents can inspect valid JSON shapes before writing files.
- Verified schema output with `schema`, a small contract assertion, `verify`, `check`, `test`, `describe`, and browser smoke. The skill Playwright client still lacks local `playwright`; Node REPL Playwright + installed Chrome works.
- Added explicit `locked` snapshot state for score-gated goals and rendered locked goals with a muted fill plus diagonal slash.
- Fixed test entity expectations to assert against `dumpState`, matching the snapshot surface agents and traces consume.
- Verified locked goal snapshots/visuals through `verify`, `check`, `test`, `describe`, `schema`, `solve`, `trace`, and browser smoke. The skill Playwright client still lacks local `playwright`; Node REPL Playwright + installed Chrome works.
- Added `simulate`, an ad hoc CLI runner for scene steps from a fixture file, JSON in shells that preserve quotes, or quote-safe shorthand such as `right:4,down:4`.
- Verified `simulate` with no-step, fixture-file, blocked shorthand, and win shorthand runs, then rechecked `verify`, `check`, `test`, `describe`, `schema`, `solve`, `trace`, and browser smoke. The skill Playwright client still lacks local `playwright`; Node REPL Playwright + installed Chrome works.
- Added `template`, which prints valid starter scene/test JSON for agents to edit instead of reconstructing file shapes from memory.
- Verified templates with direct CLI output, a core assertion that the scene validates and test steps win, `verify`, `check`, `test`, `describe`, `schema`, `solve`, `trace`, and browser smoke. The skill Playwright client still lacks local `playwright`; Node REPL Playwright + installed Chrome works.
- Added `catalog`, a JSON project map that summarizes every scene/test with validation, entity counts, goal requirements, and solver status.
- Verified catalog through `catalog`, `verify`, `check`, `test`, `describe`, `schema`, `solve`, `trace`, and browser smoke. The skill Playwright client still lacks local `playwright`; Node REPL Playwright + installed Chrome works.
- Fixed locked-goal solving so it chooses the first reachable collectible instead of failing on an alphabetically earlier unreachable decoy.
- Added `locked_exit_decoy` scene/test: `a_decoy` is isolated behind a wall, while reachable `coin` unlocks the exit.
- Verified decoy routing through `verify`, `check`, `test`, `solve`, `catalog`, and a Chrome DevTools browser smoke on `locked_exit_decoy`. The skill Playwright client still lacks local `playwright`; Chrome CDP via Node 24 works without installing dependencies.
- Fixed solver target selection so scenes with an earlier unreachable goal can still solve through a later reachable goal.
- Added `goal_decoy` scene/test, and exposed the chosen `goal` in `solve`, `verify`, `catalog`, and `describe` output for AI consumers.
- Verified multi-goal routing through `verify`, `check`, `test`, `solve`, `catalog`, `describe`, and a Chrome DevTools browser smoke on `goal_decoy`.
- Tightened validation to reject non-string `scene.name` and entity `id`, matching the published schema and avoiding bad ids in messages/catalog/test expectations.
- Added `invalid_scene_id` validation fixture.
- Verified id/name validation through `verify`, `test`, `check`, `solve`, `catalog`, `describe`, and `schema`.
- Tightened kind-specific fields so `speed` only belongs on players, `value` only on collectibles, and `requiresScore` only on goals.
- Mirrored those rules in `schema` and `describe`, and added `invalid_scene_kind_fields`.
- Verified kind-field validation through focused fixture, `verify`, `test`, `check`, `solve`, `catalog`, `describe`, and `schema`.
- Tightened optional render fields so `world.background` and entity `color` must be strings, matching `schema` and avoiding bad canvas fill styles.
- Added `invalid_scene_color` validation fixture.
- Verified color/background validation through focused fixture, `verify`, `test`, `check`, `solve`, `catalog`, `describe`, and `schema`.
- Tightened test authoring so unknown `expect` fields now fail instead of being silently ignored.
- Added `expect.testErrors` plus `invalid_test_expect_field` to prove typoed expectations are caught.
- Verified test-spec validation through focused fixture, `verify`, `test`, `check`, `solve`, `catalog`, `describe`, and `schema`.
- Tightened test authoring so unknown step fields now fail instead of being silently ignored.
- Added `invalid_test_step_field`, exposed valid step fields in `describe`/`schema`, and taught `trace` to honor `expect.testErrors`.
- Verified step-field validation through focused fixture, `verify`, `test`, `trace`, `check`, `solve`, `catalog`, `describe`, `schema`, and Chrome DevTools browser smoke on `locked_exit_decoy`.
- Tightened test authoring so invalid `action` and `actions` values now fail in `validateTest` before simulation.
- Added `invalid_test_action` to prove typoed action values are caught by `expect.testErrors`.
- Verified action-value validation through focused fixture, array-action assertion, `verify`, `test`, `trace`, `check`, `solve`, `catalog`, `describe`, `schema`, and Chrome DevTools browser smoke on `locked_exit_decoy`.
- Tightened test authoring so invalid `frames`/`seconds` values fail in `validateTest`, and schema now rejects steps that set both timing fields.
- Added `invalid_test_timing` to prove bad timing values and the silent `frames`+`seconds` case are caught by `expect.testErrors`.
- Verified timing validation through focused fixture, schema assertion, `verify`, `test`, `trace`, `check`, `solve`, `catalog`, `describe`, `schema`, and Chrome DevTools browser smoke on `locked_exit_decoy`.
- Tightened test authoring so a step cannot set both `action` and `actions`, avoiding silent single-action loss.
- Added `invalid_test_action_shape`, and schema now rejects both action fields on the same step.
- Verified action-shape validation through focused fixture, schema assertion, `verify`, `test`, `trace`, `check`, `solve`, `catalog`, `describe`, `schema`, and Chrome DevTools browser smoke with screenshot inspection on `locked_exit_decoy`.
- Tightened test authoring so invalid expectation values fail in `validateTest` instead of falling through to confusing runtime assertions or silent skips.
- Added `invalid_test_expect_values`; schema now exposes the valid snapshot entity fields for `expect.entities`.
- Verified expectation-shape validation through focused fixture, schema assertion, `verify`, `test`, `trace`, `check`, `solve`, `catalog`, `describe`, `schema`, and Chrome DevTools browser smoke with screenshot inspection on `locked_exit_decoy`.
- Tightened file-level test validation so missing or malformed `scene` references fail in `validateTest`, while internal `runTest` specs still work when the scene is passed separately.
- Added `invalid_test_scene_missing`; trace now labels missing scene references as `missing` instead of `inline`.
- Verified scene-reference validation through focused fixture, direct bad-type/internal-spec assertions, `verify`, `test`, `trace`, `check`, `solve`, `catalog`, `describe`, `schema`, `simulate`, and Chrome DevTools browser smoke with screenshot inspection on `locked_exit_decoy`.
- Tightened test authoring so unknown top-level test fields fail instead of silently allowing typos like `stepps`.
- Added `invalid_test_top_field`; schema now rejects top-level extras and `describe` exposes valid test fields.
- Verified top-level test-field validation through focused fixture, direct validator/schema/describe assertions, `verify`, `test`, `trace`, `check`, `solve`, `catalog`, `describe`, `schema`, `simulate`, and Chrome DevTools browser smoke with screenshot inspection on `locked_exit_decoy`.
- Tightened scene authoring so unknown scene, world, and entity fields fail instead of silently ignoring typos like `speeed`.
- Added `invalid_scene_unknown_fields`; schema now rejects unknown scene/world/entity fields and `describe` exposes valid scene field sets.
- Verified scene-field validation through focused fixture, direct validator/schema/describe assertion, `verify`, `test`, `trace`, `check`, `solve`, `catalog`, `describe`, `schema`, `simulate`, and Chrome DevTools browser smoke with screenshot inspection on `locked_exit_decoy`.
- Improved `catalog` so validation test entries include their `validationErrors` or `testErrors` arrays instead of only a boolean validation flag.
- Verified catalog error surfacing through a focused parse assertion, `verify`, `test`, `trace`, `check`, `solve`, `catalog`, `describe`, `schema`, `simulate`, and Chrome DevTools browser smoke with screenshot inspection on `locked_exit_decoy`.
- Tightened `expect.entities` authoring so known snapshot fields must use the right value types instead of reaching vague runtime assertions.
- Added `invalid_test_entity_expect_values`; schema now declares string/number/boolean types for expected snapshot entity fields.
- Verified expected-entity value validation through focused fixture, direct validator/schema assertion, `verify`, `test`, `trace`, `check`, `solve`, `catalog`, `describe`, `schema`, `simulate`, and Chrome DevTools browser smoke with screenshot inspection on `locked_exit_decoy`.
- Tightened malformed scene-shape validation so bad `world`, non-array `entities`, non-object entity items, and non-object scene roots return clean validation errors instead of risking iterator/property crashes.
- Added `invalid_scene_shape`.
- Verified scene-shape validation through focused fixture, direct malformed-root/entity assertions, `verify`, `test`, `trace`, `check`, `solve`, `catalog`, `describe`, `schema`, `simulate`, and Chrome DevTools browser smoke with screenshot inspection on `locked_exit_decoy`.
- Tightened grid authoring so `tileSize`, `world.width`, and `world.height` must be positive integers instead of arbitrary positive numbers.
- Added `invalid_scene_grid_fields`; schema now marks those fields as integers.
- Verified grid-field validation through focused fixture, direct validator/schema assertion, `verify`, `test`, `trace`, `check`, `solve`, `catalog`, `describe`, `schema`, `simulate`, and Chrome DevTools browser smoke with screenshot inspection on `locked_exit_decoy`.
- Tightened entity id authoring so ids must use only letters, numbers, `_`, or `-`, keeping messages, catalog keys, and `expect.entities` paths machine-friendly.
- Added `invalid_scene_id_format`; schema now declares the id pattern.
- Verified id-format validation through focused fixture, direct validator/schema assertion, `verify`, `test`, `trace`, `check`, `solve`, `catalog`, `describe`, `schema`, `simulate`, and Chrome DevTools browser smoke with screenshot inspection on `locked_exit_decoy`.
- Tightened test authoring so duplicate values inside one `actions` array fail instead of being silently collapsed.
- Added `invalid_test_action_duplicate`; schema now marks step `actions` arrays as `uniqueItems`.
- Verified duplicate-action validation through focused fixture, direct validator/schema assertion, `verify`, `test`, `trace`, `check`, `solve`, `catalog`, and Chrome DevTools browser smoke with screenshot inspection on `locked_exit_decoy`.
- Tightened `expect.entities` authoring so expected entity ids must use the same machine-friendly id format as scene entity ids.
- Added `invalid_test_entity_expect_id`; schema now declares `propertyNames` for `expect.entities`.
- Verified expected-entity id validation through focused fixture, direct validator/schema assertion, `verify`, `test`, `check`, `schema`, `solve`, `catalog`, and Chrome DevTools browser smoke with screenshot inspection on `locked_exit_decoy`.
- Exposed scene/test id patterns and expected entity field types in `describe` so agents can inspect the contract without parsing JSON Schema.
- Verified describe contract metadata through direct assertion, `describe`, `verify`, `test`, `check`, `solve`, `catalog`, and Chrome DevTools browser smoke with screenshot inspection on `locked_exit_decoy`.
- Tightened test authoring so malformed step items such as arrays fail with `steps[i] must be an object`, matching the schema.
- Added `invalid_test_step_shape` and verified it with a focused CLI fixture plus direct validator/schema assertion.
- Verified step-shape validation through `verify`, `test`, `check`, `schema`, `solve`, `catalog`, and Chrome DevTools browser smoke with screenshot inspection on `locked_exit_decoy`.
- Improved expected-error assertion messages so wrong `expect.testErrors` entries say `expected test error` instead of `expected validation error`.
- Verified expected-error labels with temporary negative CLI fixtures for both `testErrors` and `validationErrors`.
- Made CLI JSON file reads tolerate a leading UTF-8 BOM, which Windows editors and older PowerShell commands can emit.
- Added `bom_reach_exit` with an actual UTF-8 BOM and verified both BOM test and BOM scene file paths.
- Verified BOM-tolerant reads through `verify`, `test`, `check`, `schema`, `describe`, `solve`, `catalog`, and Chrome DevTools browser smoke with screenshot inspection on `locked_exit_decoy`.
- Tightened expected-error fixtures so `expect.validationErrors` and `expect.testErrors` must contain at least one expected string instead of allowing empty lists to pass too loosely.
- Added `invalid_test_expected_errors_empty`; schema now marks expected-error arrays with `minItems: 1`, and the CLI helper rejects empty expected `testErrors`.
- Verified non-empty expected-error arrays through focused fixture, direct validator/schema assertion, empty `testErrors` CLI guard, `trace`, `verify`, `test`, `check`, `schema`, `describe`, `solve`, `catalog`, and Chrome DevTools browser smoke with screenshot inspection on `locked_exit_decoy`.
- Fixed `test`/`trace` so `expect.testErrors` expectation mode only activates for arrays; malformed `expect.testErrors` now reports the validator error instead of treating a string as a list of expected characters.
- Extended `invalid_test_expect_values` to cover non-array `expect.validationErrors`, and verified malformed `expect.testErrors` with a temporary negative CLI fixture.
- Verified expected-error type handling through focused fixture, `trace`, temporary malformed `expect.testErrors` CLI check, direct validator assertion, `verify`, `test`, `check`, `schema`, `describe`, `solve`, `catalog`, and Chrome DevTools browser smoke with screenshot inspection on `locked_exit_decoy`.
- Improved `catalog` test entries with a `scene` label so agents can map fixtures to scene files without reopening each test.
- Verified catalog scene labels for normal, inline, and missing-scene tests.
- Verified catalog scene labels through `verify`, `test`, `check`, `schema`, `describe`, `solve`, `catalog`, and Chrome DevTools browser smoke with screenshot inspection on `locked_exit_decoy`.
- Tightened expected-error fixtures so `expect.validationErrors` and `expect.testErrors` are mutually exclusive instead of silently letting `testErrors` win.
- Added `invalid_test_expected_error_modes`; schema now forbids both expected-error modes in one `expect`.
- Verified mutually exclusive expected-error modes through `verify`, `test`, `check`, `schema`, `describe`, `solve`, `catalog`, and Chrome DevTools browser smoke with screenshot inspection on `locked_exit_decoy`. The skill Playwright client still lacks local `playwright`; Chrome CDP via Node 24 works without installing dependencies.
- Tightened scene authoring so player/goal overlap with hazards fails validation, matching the runtime rule that hazards trigger before goals.
- Added `invalid_scene_hazard_overlap` and verified the focused fixture plus direct validator/describe assertions.
- Verified hazard-overlap validation through `verify`, `test`, `check`, `schema`, `describe`, `solve`, `catalog`, `trace`, and Chrome DevTools browser smoke with screenshot inspection on `hazard_lane`.
- Improved `window.advanceTime(ms, actions)` so browser agents can pass CLI/test-style action arrays, action strings, or step objects in addition to the existing boolean action map.
- Verified browser action-shape normalization through `verify`, `test`, `check`, `schema`, `describe`, `solve`, `catalog`, and Chrome DevTools browser smoke with screenshot inspection on `level_01`; invalid browser actions now surface the shared `unknown action` error.
- Made browser time stepping deterministic across observation pauses: `advanceTime` now switches to manual time, `render_game_to_text` reports `timeMode`, and `setManualTime(false)`/movement/reset resume live play.
- Verified manual browser time through `verify`, `test`, `check`, `schema`, `describe`, `solve`, `catalog`, README assertion, and Chrome DevTools browser smoke with screenshot inspection on `level_01`; tick stayed fixed during a post-`advanceTime` pause and resumed after `setManualTime(false)`.
- Exposed browser hook metadata in `describeEngine()`/`npm run describe` so agents can discover `render_game_to_text`, `advanceTime`, accepted action shapes, and `setManualTime` without reading browser source.
- Verified describe browser-hook metadata through direct and CLI assertions, `verify`, `test`, `check`, `schema`, `solve`, `catalog`, README assertion, and Chrome DevTools browser smoke with screenshot inspection on `level_01`.
- Hardened the browser runner so invalid or missing scene loads keep `render_game_to_text`, `advanceTime`, and `setManualTime` available and return `status: "error"` with an `error` message instead of leaving agents with a dead page.
- Verified browser load-error handling through direct describe/README assertions, invalid-scene Chrome DevTools smoke with screenshot inspection, `verify`, `test`, `check`, `describe`, `schema`, `solve`, `catalog`, and a valid-scene Chrome DevTools smoke with screenshot inspection on `level_01`.
- Tightened browser action-map input so `advanceTime` rejects unknown boolean-map keys and non-boolean values instead of silently treating typos or strings as movement.
- Verified browser action-map validation through direct and CLI describe assertions, README assertion, Chrome DevTools smoke with screenshot inspection on `level_01`, `verify`, `test`, `check`, `schema`, `describe`, `solve`, and `catalog`.
- Tightened `simulate` shorthand parsing so malformed entries like extra colons or dangling `+` separators fail instead of being partially accepted.
- Verified strict simulate shorthand through valid/malformed shorthand checks, `verify`, `test`, `check`, `schema`, `describe`, `solve`, `catalog`, and Chrome DevTools browser smoke with screenshot inspection on `level_01`. The skill Playwright client still lacks local `playwright`; Chrome CDP via Node 24 works without installing dependencies.
- Fixed solver goal reporting so a path that wins on a different goal is rejected for the intended target instead of returning a misleading `goal`.
- Added `goal_priority` scene/test: the first listed locked goal sits beyond an earlier unlocked goal, and the solver now reports `early_exit`, the goal actually reached.
- Verified goal-priority solving through focused validator/runTest/solve assertions, `verify`, `test`, `check`, `schema`, `describe`, `solve`, `catalog`, README assertion, and Chrome DevTools browser smoke with screenshot inspection on `goal_priority`. The skill Playwright client still lacks local `playwright`; Chrome CDP via Node 24 works without installing dependencies.
- Hardened `catalog` so malformed scene files with non-array `entities` report validation errors instead of crashing while summarizing entity counts/goals.
- Verified malformed-scene catalog behavior in a temporary repo copy, then rechecked `verify`, `test`, `check`, `schema`, `describe`, `solve`, `catalog`, README assertion, and Chrome DevTools browser smoke with screenshot inspection on `level_01`. The skill Playwright client still lacks local `playwright`; Chrome CDP via Node 24 works without installing dependencies.
- Tightened `simulate` JSON step specs so typoed or full-test-shaped objects like `stepps`, `scene`, or `expect` fail instead of silently being ignored.
- Verified strict simulate JSON step specs with focused valid/invalid CLI checks, then rechecked `verify`, `test`, `check`, `schema`, `describe`, `solve`, `catalog`, and Chrome DevTools browser smoke with screenshot inspection on `level_01`. The skill Playwright client still lacks local `playwright`; Chrome CDP via Node 24 works without installing dependencies.
- Hardened `window.advanceTime(ms, actions)` so non-finite, negative, or non-number `ms` values throw `advanceTime ms must be >= 0` instead of silently no-oping or risking an infinite loop.
- Exposed the `advanceTime` ms contract in `describeEngine()` and README.
- Verified advanceTime ms validation through direct/CLI describe assertions, README assertion, Chrome DevTools browser smoke with invalid `Infinity`/negative/string `ms` checks plus screenshot inspection on `level_01`, then rechecked `verify`, `test`, `check`, `schema`, `describe`, `solve`, and `catalog`. The skill Playwright client still lacks local `playwright`; Chrome CDP via Node 24 works without installing dependencies.
- Made explicit zero-time stepping deterministic: JSON test steps with `seconds: 0` and browser `advanceTime(0, actions)` now render/record without advancing a tick, while omitted test timing still defaults to one frame.
- Added `zero_seconds_noop` to prove zero-second test steps keep `tick` and player position unchanged.
- Verified zero-time stepping through focused core/simulate/test checks, `verify`, `test`, `check`, `schema`, `describe`, `solve`, `catalog`, and Chrome DevTools browser smoke with `advanceTime(0, ["right"])` plus screenshot inspection on `level_01`. The skill Playwright client still lacks local `playwright`; Chrome CDP via Node 24 works without installing dependencies.
- Tightened positive sub-frame timing so any positive `seconds`/`ms` advances at least one frame instead of rounding down to a silent no-op.
- Added `tiny_seconds_one_frame` to prove a tiny positive `seconds` value ticks once and moves the player by one fixed frame.
- Exposed test/browser timing quantization in `describeEngine()` and README, and verified it through focused core/simulate/test/describe checks, `verify`, `test`, `check`, `schema`, `describe`, `solve`, `catalog`, and Chrome DevTools browser smoke with `advanceTime(0)`/`advanceTime(1)` plus screenshot inspection on `level_01`. The skill Playwright client still lacks local `playwright`; Chrome CDP via Node 24 works without installing dependencies.
- Tightened browser test-style action objects so `advanceTime(ms, actions)` rejects extra fields, non-array `actions`, and ambiguous `action`+`actions` instead of silently ignoring timing fields or typos.
- Verified browser action-object validation through a focused metadata/README assertion, `verify`, `test`, `check`, `schema`, `describe`, `solve`, `catalog`, and Chrome DevTools browser smoke with screenshot inspection on `level_01`; local `playwright` is still absent, so Chrome CDP via Node 24 remains the dependency-free browser path.
- Tightened browser action payload validation so explicit `null`, numbers, and booleans throw a clear `advanceTime actions` error instead of silently idling or leaking a raw TypeError.
- Verified invalid browser action payloads through focused metadata/README/source assertions, `verify`, `test`, `check`, `schema`, `describe`, `solve`, `catalog`, and Chrome DevTools browser smoke with screenshot inspection on `level_01`; the temporary server on port 5188 was stopped.
- Tightened shared action-list parsing so duplicate action values now throw `duplicate action: <name>` for browser hooks and direct helper callers instead of being silently collapsed; ad hoc `simulate` already rejected duplicate step actions through `validateTest`.
- Verified duplicate browser action handling through direct helper/metadata/README assertions, duplicate `simulate` checks, `verify`, `test`, `check`, `schema`, `describe`, `solve`, `catalog`, and Chrome DevTools browser smoke with screenshot inspection on `level_01`; the temporary server on port 5189 was stopped.
- Tightened the exported core `step(state, actions, dt)` API so direct callers get clear errors for invalid `dt`, unknown action keys, non-boolean action values, or malformed action maps instead of state corruption or truthy-string movement.
- Verified the stricter `step` boundary through direct core assertions, `verify`, `test`, `check`, `schema`, `describe`, `solve`, `catalog`, and Chrome DevTools browser smoke with screenshot inspection on `level_01`; the temporary server on port 5190 was stopped.
- Made direct `step(state, actions, 0)` match the JSON/browser zero-time contract: valid zero `dt` now returns without moving, ticking, collecting, or changing status.
- Verified direct `step(..., 0)` no-op behavior through focused core assertions, `verify`, `test`, `check`, `schema`, `describe`, `solve`, `catalog`, and Chrome DevTools browser smoke with `advanceTime(0)` plus screenshot inspection on `level_01`; the temporary server on port 5191 was stopped.
- Tightened the exported `step` runtime-state boundary so malformed direct states fail clearly instead of silently no-oping or falling into raw property errors; valid won/lost states still no-op without requiring active-world fields.
- Verified malformed direct `step` states through focused core assertions, `verify`, `test`, `check`, `schema`, `describe`, `solve`, `catalog`, and Chrome DevTools browser smoke with `advanceTime(0)` plus screenshot inspection on `level_01`; the temporary server on port 5192 was stopped.
- Tightened playing-state validation in exported `step` so malformed direct `tick`, `score`, `messages`, `world`, or entity fields fail before mutation instead of producing NaN movement, bad pushes, or raw collision math errors.
- Verified the stricter playing-state boundary through focused core assertions, `verify`, `test`, `check`, `schema`, `describe`, `solve`, `catalog`, and Chrome DevTools browser smoke with `advanceTime(0)` plus screenshot inspection on `level_01`; the temporary server on port 5193 was stopped.
- Moved exported `step` zero-dt no-op behind playing-state validation, so malformed `playing` states still fail clearly when an agent calls `step(state, actions, 0)` for observation; valid `won`/`lost` states still no-op without active-world fields.
- Verified zero-dt playing-state validation through focused core assertions, `verify`, `test`, `check`, `schema`, `describe`, `solve`, `catalog`, and Chrome DevTools browser smoke with `advanceTime(0)` plus screenshot inspection on `level_01`; the temporary server on port 5194 was stopped.
- Tightened exported `actionsFromList(list)` so direct callers get `actions list must be an array` for strings or object-like inputs instead of misleading per-character `unknown action` errors; exposed the helper contract in `describe` and README.
- Verified the action-list boundary through focused helper/metadata/README assertions, `verify`, `test`, `check`, `schema`, `describe`, `solve`, `catalog`, and Chrome DevTools browser smoke covering array plus string browser action shapes with screenshot inspection on `level_01`; the temporary server on port 5195 was stopped.
- Tightened exported `dumpState(state)` so direct callers get clear state/snapshot errors for malformed roots, missing ids, or non-machine-friendly ids instead of raw spread/map failures; exposed the snapshot helper contract in `describe` and README.
- Verified the snapshot boundary through focused dumpState/step/metadata/README assertions, `verify`, `test`, `check`, `schema`, `describe`, `solve`, `catalog`, and Chrome DevTools browser smoke confirming `render_game_to_text` still snapshots and wins on `level_01`; the temporary server on port 5196 was stopped.
- Aligned runtime entity validation with scene kind-field rules, so direct `step`/`dumpState` states reject `speed` outside players, `value`/`collected` outside collectibles, and `requiresScore` outside goals instead of leaking confusing snapshot or simulation state.
- Verified runtime kind-field validation through focused `step`/`dumpState` assertions, `verify`, `test`, `check`, `schema`, `describe`, `solve`, `catalog`, and Chrome DevTools browser smoke confirming `render_game_to_text` still snapshots and wins on `level_01`; the temporary server on port 5197 was stopped.
- Tightened runtime entity validation so direct `step`/`dumpState` states reject unknown entity fields like `speeed`/`valu` and non-string `color`, matching scene strictness for typo-prone AI-authored state objects.
- Verified runtime unknown-field validation through focused direct API assertions, `verify`, `test`, `check`, `schema`, `describe`, `solve`, `catalog`, and Chrome DevTools browser smoke confirming `render_game_to_text` still snapshots and wins on `level_01`; the temporary server on port 5198 was stopped.
- Aligned runtime entity bounds with scene validation, so direct `step`/`dumpState` states reject missing/invalid world dimensions and off-world entities instead of allowing bad snapshots or clamped simulation surprises.
- Verified runtime bounds validation through focused direct API assertions, `verify`, `test`, `check`, `schema`, `describe`, `solve`, `catalog`, and Chrome DevTools browser smoke confirming `render_game_to_text` still snapshots and wins on `level_01`; the temporary server on port 5199 was stopped.
- Tightened runtime message validation so direct `step`/`dumpState` states reject non-string `messages` entries instead of leaking malformed snapshots into traces, browser text, or tests.
- Verified runtime message validation through focused direct API assertions, `verify`, `test`, `check`, `schema`, `describe`, `solve`, `catalog`, and Chrome DevTools browser smoke confirming string-only `render_game_to_text` messages and the win route on `level_01`; the temporary server on port 5200 was stopped.
- Tightened exported `runTest(scene, test, onStep)` so non-function `onStep` values fail immediately with `runTest onStep must be a function` instead of throwing a raw optional-call TypeError during trace emission; exposed the helper contract in `describe` and README.
- Verified the `runTest` callback boundary through focused direct API/metadata/README assertions, `verify`, `test`, `check`, `schema`, `describe`, `solve`, `catalog`, and Chrome DevTools browser smoke confirming the win route on `level_01`; the temporary server on port 5201 was stopped.
- Tightened runtime world validation so direct `step`/`dumpState` states reject unknown `world` fields and non-string `background`, matching scene validation for typo-prone AI-authored state objects.
- Verified runtime world-field validation through focused direct API assertions, `verify`, `test`, `check`, `schema`, `describe`, `solve`, `catalog`, and Chrome DevTools browser smoke confirming the win route on `level_01`; the temporary server on port 5202 was stopped.
- Reconfirmed locked-goal decoy routing: `solveScene` solves `locked_exit_decoy` by collecting reachable `coin` while leaving alphabetically earlier unreachable `a_decoy` untouched.
- Verified that routing with a focused solver assertion, `verify`, `test`, `check`, `solve`, `catalog`, and Chrome DevTools browser smoke on `locked_exit_decoy`; the temporary server on port 5203 was stopped.
- Tightened `simulate` shorthand parsing so empty comma segments like `right:1,,down:1`, `,right:1`, and `right:1,` fail instead of being silently discarded.
- Verified empty-segment shorthand validation through focused CLI checks, `verify`, `test`, `check`, `schema`, `describe`, `solve`, `catalog`, and Chrome DevTools browser smoke confirming the win route on `level_01`; the temporary server on port 5204 was stopped.
- Relaxed `simulate` shorthand formatting so AI/human-authored input with spaces around commas, colons, or `+` separators works, while blank actions and blank segments still fail.
- Verified spaced shorthand through focused valid/invalid CLI checks, `verify`, `test`, `check`, `schema`, `describe`, `solve`, `catalog`, and Chrome DevTools browser smoke confirming the win route on `level_01`; the temporary server on port 5205 was stopped.
- Tightened `run` CLI arg parsing so unknown flags and invalid `--port` values fail instead of silently falling back to port 5173.
- Verified `run` arg parsing with focused invalid-port/unknown-flag checks, valid custom-port startup, `verify`, `test`, `check`, `schema`, `describe`, `solve`, `catalog`, and Chrome DevTools browser smoke confirming the win route on `level_01`; the temporary server on port 5207 was stopped.
- Fixed `simulate` command argument forwarding so spaced shorthand split across shell tokens like `right:1, down:1` is joined and parsed instead of losing later tokens.
- Verified full step-spec forwarding with no-step, split-spaced shorthand, and missing-comma negative checks, plus `verify`, `test`, `check`, `schema`, `describe`, `solve`, `catalog`, and Chrome DevTools browser smoke confirming the win route on `level_01`; the temporary server on port 5208 was stopped.
- Fixed `run` flag-first invocation so `node bin/engine.js run --port 5200` uses the default `level_01` scene instead of treating `--port` as a scene path.
- Verified flag-first `run` with default-scene startup, custom-scene startup, unknown-flag failure, `verify`, `test`, `check`, `schema`, `describe`, `solve`, `catalog`, and Chrome DevTools browser smoke confirming the default scene win route; the temporary server on port 5211 was stopped.
- Tightened `run --port` parsing so duplicate `--port` flags fail instead of silently using the last value.
- Verified duplicate-port rejection, invalid-port rejection, valid flag-first startup, `verify`, `test`, `check`, `schema`, `describe`, `solve`, `catalog`, and Chrome DevTools browser smoke confirming the default scene win route; the temporary server on port 5213 was stopped.
- Tightened fixed-arity CLI commands so `describe`, `schema`, `catalog`, `verify`, `template`, and `dump-state` reject stray extra arguments instead of silently ignoring pasted tokens.
- Verified fixed-arity argument guards with focused negative/default checks, `verify`, `test`, `check`, `schema`, `describe`, `solve`, `catalog`, and Chrome DevTools browser smoke confirming the default scene win route; the temporary server on port 5214 was stopped.
- Improved CLI JSON parse diagnostics so malformed JSON reports `invalid JSON in <path>: ...` instead of a filename-free parser error; UTF-8 BOM handling is preserved.
- Verified JSON diagnostics with a temporary malformed file, BOM fixture check, `verify`, `test`, `check`, `schema`, `describe`, `solve`, `catalog`, and Chrome DevTools browser smoke confirming the default scene win route; the temporary server on port 5215 was stopped.
- Improved `simulate` inline step JSON diagnostics so malformed pasted JSON reports `invalid step JSON: ...` instead of a raw parser error; shorthand and valid inline JSON still work.
- Verified inline step JSON diagnostics through exact-argv CLI checks, `verify`, `test`, `check`, `schema`, `describe`, `solve`, `catalog`, and Chrome DevTools browser smoke confirming the default scene win route; the temporary server on port 5216 was stopped.
- Improved `run` listen-error diagnostics so a busy port reports `run --port <port> is already in use` instead of an unhandled Node server error stack.
- Verified busy-port diagnostics, valid runner startup, `verify`, `test`, `check`, `schema`, `describe`, `solve`, `catalog`, and Chrome DevTools browser smoke confirming the default scene win route; the temporary server on port 5219 was stopped.
- Improved CLI JSON read diagnostics so missing or unreadable JSON files report `cannot read JSON <path>: <code>` instead of a raw absolute-path filesystem error; malformed JSON and BOM behavior are unchanged.
- Verified missing-file, malformed-file, and BOM JSON read paths, plus `verify`, `test`, `check`, `schema`, `describe`, `solve`, `catalog`, and Chrome DevTools browser smoke confirming the default scene win route; the temporary server on port 5220 was stopped.
- Made bulk reports resilient to malformed scene/test JSON: `verify`, `catalog`, and `trace` now keep scanning and emit per-file `ok: false` entries instead of aborting the whole report. Single-target commands still fail loudly.
- Verified bulk malformed JSON reporting with temporary bad scene/test files, then rechecked `verify`, `test`, `check`, `trace`, `catalog`, `describe`, and Chrome DevTools browser smoke confirming the default scene win route; the temporary server on port 5221 was stopped.
- Fixed `trace` for valid tests that reference missing or malformed scene files, so those now return per-test `ok: false` entries and later trace entries still run.
- Verified missing/malformed referenced scene handling with temporary test/scene files, then rechecked `verify`, `test`, `check`, `trace`, `catalog`, `describe`, `solve`, `schema`, and Chrome DevTools browser smoke confirming the default scene win route; the temporary server on port 5222 was stopped.
- Made `solve` resilient across multiple scene inputs: missing, malformed, or invalid scenes now print per-scene `ok: false` results and later scenes still solve.
- Verified solve continuation with temporary missing/malformed scene inputs, then rechecked `verify`, `test`, `check`, `solve`, `describe`, `catalog`, `trace`, `schema`, and Chrome DevTools browser smoke confirming the default scene win route; the temporary server on port 5223 was stopped.
- Made `test` run all requested fixtures instead of stopping at the first failure; failing fixtures now print `fail <path> <error>` and leave the process exit non-zero after the run.
- Verified test continuation with a missing test path followed by a passing fixture, then rechecked `verify`, `test`, `check`, `solve`, `describe`, `catalog`, `trace`, `schema`, and Chrome DevTools browser smoke confirming the default scene win route; the temporary server on port 5224 was stopped.
- Made `solve` print one parseable JSON array for the whole run instead of a stream of top-level JSON objects, including single-scene invocations.
- Verified solve-array parsing with a missing scene followed by a passing scene, then rechecked `verify`, `test`, `check`, `solve`, `describe`, `catalog`, `trace`, `schema`, and Chrome DevTools browser smoke confirming the default scene win route; the temporary server on port 5225 was stopped.
- Made `simulate` print parseable `ok: false` JSON for bad scene paths, malformed step specs, and runtime errors instead of falling back to stderr-only command failure.
- Verified simulate success and failure JSON parsing, then rechecked `verify`, `test`, `check`, `solve`, `simulate`, `describe`, `catalog`, `trace`, `schema`, and Chrome DevTools browser smoke confirming the default scene win route; the temporary server on port 5226 was stopped.
- Made `check` report every requested scene file instead of stopping at the first missing, malformed, or invalid scene; `run` still fails before serving an invalid scene.
- Verified check continuation with a missing scene followed by a passing scene, then rechecked `verify`, `test`, `check`, invalid-scene `run`, `solve`, `simulate`, `describe`, `catalog`, `trace`, `schema`, and Chrome DevTools browser smoke confirming the default scene win route; the temporary server on port 5227 was stopped.
- Aligned `catalog` scene `ok` with `verify`: scenes are `ok` only when valid and solvable, while the new `valid` field preserves validation-only status for AI scene selection.
- Verified the catalog distinction with a temporary valid-but-unsolvable scene in a copied repo, then rechecked `verify`, `test`, `check`, `solve`, `simulate`, `describe`, `catalog`, `trace`, `schema`, and Chrome DevTools browser smoke confirming the default scene win route; the temporary server on port 5228 was stopped.
- Added a dedicated duplicate entity id validation fixture and exposed the existing unique-id rule in `describe` and README so AI-authored scenes do not create ambiguous ids for messages, catalog rows, or `expect.entities`.
- Verified duplicate-id validation through the focused fixture and contract assertion, then rechecked `verify`, `test`, `check`, `solve`, `simulate`, `describe`, `catalog`, `trace`, `schema`, and Chrome DevTools browser smoke confirming the default scene win route; the temporary server on port 5229 was stopped.
- Tightened runtime state validation so direct `step` and `dumpState` calls reject duplicate entity ids before movement or snapshotting, matching scene validation for mutated AI-authored states.
- Verified runtime duplicate-id rejection through direct API assertions for both `step` and `dumpState`, then rechecked `verify`, `test`, `check`, `solve`, `simulate`, `describe`, `catalog`, `trace`, `schema`, and Chrome DevTools browser smoke confirming the default scene win route; the temporary server on port 5230 was stopped.
- Tightened runtime state validation so direct `step` and `dumpState` calls reject mutated states with zero/multiple players or no goals, matching scene validation before movement or snapshotting.
- Verified runtime player/goal count rejection through direct API assertions for both `step` and `dumpState`, then rechecked `verify`, `test`, `check`, `solve`, `simulate`, `describe`, `catalog`, `trace`, `schema`, and Chrome DevTools browser smoke confirming the default scene win route; the temporary server on port 5231 was stopped.
- Tightened emitted JSON Schema so scene `entities` now requires exactly one player and at least one goal, matching validator/runtime count rules for AI authors using `npm run schema`.
- Verified schema player/goal count constraints with a direct schema assertion plus count-adjacent validation fixtures, then rechecked `verify`, `test`, `check`, `solve`, `simulate`, `describe`, `catalog`, `trace`, `schema`, and Chrome DevTools browser smoke confirming the default scene win route; the temporary server on port 5232 was stopped.
- Tightened runtime root-state validation so direct `step` and `dumpState` calls reject mutated playing/snapshot states with blank scene labels or invalid `tileSize` values before movement or snapshotting.
- Verified runtime scene/tileSize rejection through direct API assertions for both `step` and `dumpState`, then rechecked `verify`, `test`, `check`, `solve`, `simulate`, `describe`, `catalog`, `trace`, `schema`, and Chrome DevTools browser smoke confirming the default scene win route; the temporary server on port 5233 was stopped.
- Tightened runtime world validation so direct `step` and `dumpState` calls require positive integer `world.width` and `world.height`, matching scene/schema grid rules for mutated AI-authored states.
- Verified runtime world-dimension rejection through direct API assertions for both `step` and `dumpState`, then rechecked `verify`, `test`, `check`, `solve`, `simulate`, `describe`, `catalog`, `trace`, `schema`, and Chrome DevTools browser smoke confirming the default scene win route; the temporary server on port 5234 was stopped.
- Added `xXolotlValidation` annotations to `schema` output for scene rules that standard JSON Schema cannot enforce cleanly: unique entity ids, world-relative bounds, and player/goal overlap blockers.
- Verified schema annotations through direct schema assertions plus validator-only scene fixtures, then rechecked `verify`, `test`, `check`, `solve`, `simulate`, `describe`, `catalog`, `trace`, `schema`, and Chrome DevTools browser smoke confirming the default scene win route; the temporary server on port 5235 was stopped.
- Exposed existing player/goal count, entity shape, and world-bound validation rules in `describe` output so agents can inspect the scene contract without diffing validator code or schema annotations.
- Verified the richer `describe` rules through direct metadata assertions plus adjacent invalid-scene fixtures, then rechecked `verify`, `test`, `check`, `solve`, `simulate`, `describe`, `catalog`, `trace`, `schema`, and Chrome DevTools browser smoke confirming the default scene win route; the temporary server on port 5237 was stopped.
- Fixed the `template` npm script so `npm run template` still prints the scene starter while `npm run template -- test` now reaches the CLI's existing test template instead of passing two template kinds.
- Verified both npm template modes plus `describe` metadata, then rechecked `verify`, `test`, `check`, `solve`, `simulate`, `template`, `describe`, `catalog`, `trace`, `schema`, and Chrome DevTools browser smoke confirming the default scene win route; the temporary server on port 5238 was stopped.
- Fixed the `dump-state` npm script so `npm run dump-state` still snapshots the default scene while `npm run dump-state -- scenes/coin_room.json` now reaches the CLI's existing custom scene argument.
- Verified default and custom npm dump snapshots, then rechecked `verify`, `test`, `check`, `solve`, `simulate`, `dump-state`, `template`, `describe`, `catalog`, `trace`, `schema`, and Chrome DevTools browser smoke confirming the default scene win route; the temporary server on port 5239 was stopped.
- Fixed the `simulate` npm script so `npm run simulate` still runs the default scene while `npm run simulate -- scenes/coin_room.json right:4` now reaches the CLI's existing custom scene and shorthand arguments.
- Verified default, custom, and invalid npm simulate paths, then rechecked `verify`, `test`, `check`, `solve`, `simulate`, `dump-state`, `template`, `describe`, `catalog`, `trace`, `schema`, and Chrome DevTools browser smoke confirming the default scene win route; the temporary server on port 5240 was stopped.
- Fixed the `run` npm script so `npm run run` still starts the default scene while `npm run run -- scenes/coin_room.json --port 5200` now reaches the CLI's existing custom scene and port arguments.
- Verified default and custom npm run startup forms with explicit child-server cleanup, then rechecked `verify`, `test`, `check`, `solve`, `simulate`, `dump-state`, `template`, `run`, `describe`, `catalog`, `trace`, `schema`, and Chrome DevTools browser smoke confirming the default scene win route; the temporary server on port 5249 was stopped.
- Exposed browser runner command, default scene, default port, and URL shape in `describe` output so agents can discover how to launch and address the browser runner without reading README text.
- Verified browser runner metadata through focused `describe`/README assertions, then rechecked `verify`, `test`, `check`, `solve`, `simulate`, `dump-state`, `template`, `run`, `describe`, `catalog`, `trace`, `schema`, and Chrome DevTools browser smoke confirming the default scene win route; the temporary server on port 5252 was stopped.
- Exposed `dump-state` command and default scene metadata in `describe` output so agents can discover the snapshot CLI without reading README text.
- Verified dump-state metadata through focused `describe`/README assertions plus default/custom snapshot commands, then rechecked `verify`, `test`, `check`, `solve`, `simulate`, `dump-state`, `template`, `run`, `describe`, `catalog`, `trace`, `schema`, and Chrome DevTools browser smoke confirming the default scene win route; the temporary server on port 5255 was stopped.
- Exposed `schema` command metadata in `describe` output, including scene/test outputs, JSON Schema draft version, and scene annotations.
- Verified schema metadata through focused `describe`/schema/README assertions, then rechecked `verify`, `test`, `check`, `solve`, `simulate`, `dump-state`, `template`, `run`, `describe`, `catalog`, `trace`, `schema`, and Chrome DevTools browser smoke confirming the default scene win route; the temporary server on port 5258 was stopped.
- Reconfirmed the requested locked-goal decoy slice: `solveScene` routes to the reachable `coin` in `locked_exit_decoy` and leaves alphabetically earlier unreachable `a_decoy` uncollected.
- Verified that resumed slice with `verify`, `test`, `solve`, `catalog`, and Chrome DevTools browser smoke on `locked_exit_decoy`; the temporary server on port 5261 was stopped.
- Exposed the existing `simulate` argument/default/shorthand contract in `describeEngine()` so agents can discover custom scenes, step JSON/files, shorthand, combined actions, and wait aliases without reading CLI source.
- Verified simulate metadata with focused `describe`/README assertions plus real shorthand and `wait:1` runs, then rechecked `verify`, `test`, `check`, `solve`, `catalog`, and Chrome DevTools browser smoke on the default runner; the temporary server on port 5262 was stopped.
- Fixed `simulate` default-scene step invocation so `npm run simulate -- right:1` and inline step JSON now run against `scenes/level_01.json` instead of treating the step spec as a scene path; `.json` first args remain scene paths, so step files go after an explicit scene.
- Added `tests/cli_simulate_default_steps.mjs` to keep that CLI boundary covered by `npm test`, then verified focused simulate/default/custom checks plus `verify`, `test`, `check`, `solve`, `catalog`, and Chrome DevTools browser smoke on the default runner; the temporary server on port 5263 was stopped.
- Exposed the new `npm test` coverage shape in `describeEngine()` and README: JSON fixtures plus the CLI simulate smoke, so agent-facing metadata matches the actual test script.
- Verified that metadata through focused `describe`/README/`npm test` assertions, then rechecked `verify`, `check`, `solve`, `catalog`, `schema`, and Chrome DevTools browser smoke on the default runner; the temporary server on port 5264 was stopped.
- Added a browser fullscreen control: `f` toggles native fullscreen, `Esc` exits, canvas CSS now preserves scene aspect instead of hard-coding 5:3, and `describe`/README expose browser controls.
- Verified fullscreen/control metadata with focused assertions, `npm test`/CLI gates, and Chrome DevTools browser smoke that stubs fullscreen, wins the default scene, and inspects the screenshot; the temporary server on port 5265 was stopped.
- Exposed and tested the existing default test-step timing contract: when a step omits both `frames` and `seconds`, it advances one fixed frame.
- Verified default test-step timing with a focused fixture/metadata check, `verify`, `test`, `check`, `solve`, `catalog`, `schema`, `trace`, `simulate`, and Chrome DevTools browser smoke that advanced one default frame before winning the default scene; the temporary server on port 5266 was stopped.
- Added `xXolotlValidation` annotations to the emitted test schema for timing defaults, positive-second quantization, and expected-error matching, with the existing CLI smoke now checking the timing annotation.
- Verified test schema annotations with focused schema/describe assertions, `npm test`, `verify`, `check`, `solve`, `catalog`, `schema`, `trace`, `simulate`, and Chrome DevTools browser smoke after resetting into manual time and winning the default scene; the temporary server on port 5268 was stopped.
- Tightened the browser runner serve surface so it only serves `web/*`, `scenes/*`, and `src/*`; repo files like `package.json`, `.git/*`, and `progress.md` now return 403, with a CLI smoke covering the allowed and blocked paths.
- Verified the serve-surface lock with focused metadata/docs checks, `npm test`, `verify`, `check`, `solve`, `catalog`, `schema`, `trace`, `simulate`, and Chrome DevTools browser smoke that won the default scene and confirmed `/package.json` returns 403 from the page; the temporary server on port 5269 was stopped.
- Aligned `describeEngine().tests.includes` and README with the current `npm test` script: JSON fixtures plus the default-simulate and runner serve-surface CLI smoke files.
- Added `docs/agent-contract.md` and `docs/session-summary.md`, linked them from README, and documented the current scene/test/CLI/browser handoff for the pushed session.

TODO:
- Add the next engine primitive only when a scene/test needs it; current useful primitives are player, solid, hazard, goal, collectible, decor.
